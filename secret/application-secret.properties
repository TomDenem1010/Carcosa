# Template for secret-specific application properties.
# 
# Do NOT commit real secret values to version control. This file should serve
# only as an example. At runtime, secrets should be provided via:
#   - Environment variables
#   - CI/Kubernetes secrets
#   - Externalized configuration not checked into the repo
#
# Example (uncomment and set via environment substitution if needed):
# spring.data.mongodb.uri=${MONGODB_URI}
